{
  "name": "youtube-cards-action",
  "version": "1.0.0",
  "description": "GitHub Action — generates a beautiful YouTube video card SVG for your README",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --license licenses.txt",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
